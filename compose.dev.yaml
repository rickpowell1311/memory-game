services:
  server:
    env_file:
      - ./.env
    extends:
      file: common-services.yaml
      service: server
  db:
    env_file:
    - ./.env
    extends:
      file: common-services.yaml
      service: db
    volumes:
      - db-data:/var/lib/postgresql/data
volumes:
  db-data:

